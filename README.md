# Online Voting System

## Prerequisites
- Java 17 or later
- Maven (optional, wrapper included)

## How to Run
1. Open a terminal in the project root.
2. Run the following command:
   - **Windows (Command Prompt):**
     ```cmd
     mvnw.cmd spring-boot:run
     ```
   - **Windows (PowerShell):**
     ```powershell
     .\mvnw.cmd spring-boot:run
     ```
   - **Mac/Linux:**
     ```bash
     ./mvnw spring-boot:run
     ```

3. Once started, access the application at:
   [http://localhost:8080](http://localhost:8080)

## Login Credentials
(Add default credentials here if found in source code, otherwise leave blank or generic)
